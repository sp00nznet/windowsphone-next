<Window x:Class="WindowsPhoneNext.Mahjong.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Mahjong"
        Width="720" Height="1560"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}"
        KeyDown="Window_KeyDown"
        Loaded="Window_Loaded">

    <!-- Bezel-safe container with curved corners -->
    <Border Style="{StaticResource BezelWindowBorderStyle}"
            Background="{StaticResource BackgroundBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="80"/>
            </Grid.RowDefinitions>

            <!-- Header with bezel-safe margins -->
            <Border Grid.Row="0" Style="{StaticResource BezelStatusBarStyle}" Background="{StaticResource SurfaceBrush}">
                <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                       Content="&#x2190;"
                       Background="Transparent"
                       Foreground="{StaticResource TextPrimaryBrush}"
                       BorderThickness="0"
                       FontSize="24"
                       Cursor="Hand"
                       Click="CloseButton_Click"/>

                <TextBlock Grid.Column="1"
                          Text="Mahjong Solitaire"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          FontSize="20"
                          FontWeight="SemiBold"
                          Foreground="{StaticResource TextPrimaryBrush}"/>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <TextBlock Text="Tiles: "
                              FontSize="14"
                              Foreground="{StaticResource TextSecondaryBrush}"
                              VerticalAlignment="Center"/>
                    <TextBlock x:Name="TilesText"
                              Text="144"
                              FontSize="14"
                              FontWeight="SemiBold"
                              Foreground="{StaticResource AccentBrush}"
                              VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Game Area -->
        <Canvas x:Name="GameCanvas" Grid.Row="1" ClipToBounds="True"/>

        <!-- Bottom Controls -->
        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}">
            <Grid Margin="16,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                       Style="{StaticResource GameButtonStyle}"
                       Content="New Game"
                       Click="NewGame_Click"
                       Margin="4"/>

                <Button Grid.Column="1"
                       Style="{StaticResource GameButtonStyle}"
                       Content="Hint"
                       Click="Hint_Click"
                       Margin="4"/>

                <Button Grid.Column="2"
                       Style="{StaticResource GameButtonStyle}"
                       Content="Shuffle"
                       Click="Shuffle_Click"
                       Margin="4"/>
            </Grid>
        </Border>

        <!-- Win Overlay -->
        <Border x:Name="WinOverlay"
               Grid.RowSpan="3"
               Background="#E0000000"
               Visibility="Collapsed">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="You Win!"
                          FontSize="48"
                          FontWeight="Bold"
                          Foreground="{StaticResource AccentBrush}"
                          HorizontalAlignment="Center"/>
                <TextBlock x:Name="WinTimeText"
                          Text="Time: 0:00"
                          FontSize="18"
                          Foreground="{StaticResource TextSecondaryBrush}"
                          HorizontalAlignment="Center"
                          Margin="0,16,0,32"/>
                <Button Style="{StaticResource GameButtonStyle}"
                       Content="Play Again"
                       Click="NewGame_Click"
                       Padding="32,16"
                       FontSize="18"/>
            </StackPanel>
        </Border>

        <!-- No Moves Overlay -->
        <Border x:Name="NoMovesOverlay"
               Grid.RowSpan="3"
               Background="#E0000000"
               Visibility="Collapsed">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="No More Moves!"
                          FontSize="36"
                          FontWeight="Bold"
                          Foreground="#EF4444"
                          HorizontalAlignment="Center"/>
                <TextBlock Text="Try shuffling the remaining tiles"
                          FontSize="16"
                          Foreground="{StaticResource TextSecondaryBrush}"
                          HorizontalAlignment="Center"
                          Margin="0,16,0,32"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Style="{StaticResource GameButtonStyle}"
                           Content="Shuffle"
                           Click="ShuffleFromOverlay_Click"
                           Padding="24,12"
                           Margin="8,0"/>
                    <Button Style="{StaticResource GameButtonStyle}"
                           Content="New Game"
                           Click="NewGame_Click"
                           Padding="24,12"
                           Margin="8,0"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>
